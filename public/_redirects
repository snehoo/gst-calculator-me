# Cloudflare Pages handles HTTP→HTTPS and www→non-www redirects automatically
# via dashboard configuration, so no explicit redirects needed here.
#
# Since all routes are pre-rendered with SSG (34 routes with their own HTML files),
# there is no need for a catch-all SPA fallback rule.
