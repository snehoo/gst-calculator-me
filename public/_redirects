# Cloudflare Pages requires www→non-www redirects to be configured via:
# 1. Cloudflare Worker/Function for domain-level redirects, OR
# 2. Cloudflare Dashboard Page Rules, OR
# 3. Google Search Console preferred domain setting
#
# For now, ensure preferred domain is set in Search Console to:
# https://gstcalculator.me (without www)
